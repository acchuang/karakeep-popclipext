# Karakeep-popclipext

## Purpose

PopClip extension for saving to Karakeep. macOS-only.

## Ownership

Independent PopClip extension. Two files: `Config.json` + `Extension.js`.

## Local Contracts

- PopClip extension format. Install by double-clicking or dropping into PopClip.
- macOS only. No cross-platform concerns.
